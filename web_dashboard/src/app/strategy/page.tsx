'use client';
import React from 'react';

export default function StrategyPage(){
  // Si tu exposes /config (ta stratégie y figure), on l’affiche/édite ici plus tard
  return (
    <div className="container">
      <div className="card"><h3>Strategy</h3><div className="sub">Bientôt : affichage/édition des paramètres de stratégie (/config)</div></div>
    </div>
  );
}
